!!!
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
  %body
  .container
    %h2 Posts by #{params[:author_name]}
    %table.table.table-striped.sortable
      %thead
        %tr
          %th Title
          %th{:colspan => "3"}
      %tbody
        - @posts.each do |post|
          %tr
            %td
              = post.title
    %br/
    = link_to 'Back', authors_path
